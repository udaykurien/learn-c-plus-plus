#include <iostream>

int main () {

    /*
    Introducing std::string (14:01:26):
    Its a high level type that shields the user from typical complexities when working with strings (eg. null character)

    With std::string we can do many of the things we were doing with cstrings, such as concatenation, search, copy etc
    */

   return 0;
}