#include <iostream>

int main() {
    /*
    References:
    These are a way to set up aliases for variables
    eg.
    var name    var type    mem addr    value
    var1        int         0x12ab      33
    var1_alias  int         0x12ab      33
    var2        int         0x15fa      33

    - var1_alias is an alias of var1, because it references the same memory location that var1 references.
    var2 is not an alias of var1 because it references a different memory location.

    - Any changes to the value of var1, will also be reflected in its alias, var1_alias.
    */

   return 0;
}